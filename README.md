# Stormrider

(WIP) C++ HTTP Client.   
GET: Currently about 35% faster than requests over 1000 calls.

How to build:   
`mkdir build`   
`cd build`    
`cmake ..`   
`cmake --build .`   
`cd ..`   
`python3 call.py`   
